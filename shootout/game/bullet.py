from game.actor import Actor

class Bullet(Actor):
    def __init__(self):
        """
        The velocity (or direction) of bullet, has to come from the player. 
        (the direction they are facing should be the same the bullet is going)
        """
        super().__init__(self)
        pass
        
    pass

